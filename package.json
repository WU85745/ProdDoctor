{
  "name": "proddoctor",
  "version": "1.4.1",
  "description": "Post-deploy checks for real production domains with optional browser evidence, traces and HTML reports.",
  "type": "module",
  "bin": {
    "proddoctor": "./bin/proddoctor.mjs"
  },
  "scripts": {
    "start": "node ./bin/proddoctor.mjs",
    "test": "node --test",
    "check": "node --check ./src/checker.mjs && node --check ./src/assets.mjs && node --check ./src/tls.mjs && node --check ./src/browser.mjs && node --check ./src/report.mjs && node --check ./src/report-en.mjs && node --check ./src/html-report.mjs && node --check ./src/html-report-en.mjs && node --check ./bin/proddoctor.mjs && node --check ./scripts/sync-version.mjs && node --check ./scripts/check-release-policy.mjs && node --check ./scripts/check-pr-title.mjs && npm run version:check && npm test",
    "version:check": "node ./scripts/sync-version.mjs --check",
    "release:sync": "node ./scripts/sync-version.mjs --write",
    "release:policy": "node ./scripts/check-release-policy.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "production",
    "health-check",
    "github-actions",
    "cloudflare",
    "deployment",
    "smoke-test",
    "tls",
    "devops",
    "playwright",
    "browser-testing",
    "trace",
    "incident-debugging"
  ],
  "license": "MIT"
}
