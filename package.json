{
  "name": "proddoctor",
  "version": "0.3.0",
  "description": "Post-deploy checks for real production domains with optional Playwright browser validation.",
  "type": "module",
  "bin": {
    "proddoctor": "./bin/proddoctor.mjs"
  },
  "scripts": {
    "start": "node ./bin/proddoctor.mjs",
    "test": "node --test",
    "check": "node --check ./src/checker.mjs && node --check ./src/assets.mjs && node --check ./src/tls.mjs && node --check ./src/browser.mjs && node --check ./src/report.mjs && node --check ./bin/proddoctor.mjs && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "production",
    "health-check",
    "github-actions",
    "cloudflare",
    "deployment",
    "smoke-test",
    "tls",
    "devops",
    "playwright",
    "browser-testing"
  ],
  "license": "MIT"
}
