name: Smoke test GitHub Action

on:
  push:
    branches: [main]
    paths:
      - action.yml
      - bin/**
      - src/**
      - test/**
      - .github/workflows/action-smoke.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Minimal usage without expect
        uses: ./
        with:
          url: https://example.com
          retries: 1
          timeout: 15000

      - name: Usage with expected page text
        uses: ./
        with:
          url: https://example.com
          expect: Example Domain
          retries: 1
          timeout: 15000
