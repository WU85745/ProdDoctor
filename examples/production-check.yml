# 用户示例：默认锁定具体版本 v1.4.1。
# 生产流水线建议把 v1.4.1 替换为该 tag 对应的完整 commit SHA。

name: Verify real production

on:
  workflow_dispatch:
  deployment_status:

jobs:
  verify:
    if: github.event_name != 'deployment_status' || github.event.deployment_status.state == 'success'
    runs-on: ubuntu-latest

    steps:
      - uses: lucaswenbo/ProdDoctor@v1.4.1
        with:
          url: https://example.com
          expect: Example Domain
          status: 200
          retries: 2
          timeout: 15000
          check_assets: true
          max_assets: 20
          tls_warn_days: 14
